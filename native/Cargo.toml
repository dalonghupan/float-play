[package]
name = "floatplay-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
ffmpeg-next = "7.1"
ffmpeg-sys-next = { version = "7.1", features = ["build"] }
cpal = "0.15"

[build-dependencies]
pkg-config = "0.3"
